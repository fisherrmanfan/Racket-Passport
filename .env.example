# Postgres behind the racket_database_etl pipeline (Supabase pooler URL).
# Read by both the Next.js route handlers and the legacy Express server.
DATABASE_URL="postgresql://user:password@host:6543/postgres"

# Legacy Express API only (legacy/src/server.js). Next dev uses 3000.
PORT=4000
